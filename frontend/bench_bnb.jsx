import React from 'react';
import ReactDOM from 'react-dom';


document.addEventListener('DOMContentLoada', () => {
  const root = document.getElementById('root');
  ReactDOM.render(<Root />, root);
});
